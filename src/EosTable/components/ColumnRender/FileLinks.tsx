import React from 'react';
import { IControlRenderProps } from '../../types/IControlRenderProps';

export default function FileLinksDisplay({}:IControlRenderProps) {
    return <p style={{ textAlign: "center" }}>{"Здесь будут линки"}</p>
}